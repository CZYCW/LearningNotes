### What is DNS
DNS is the Internet's naming service that maps human-friendly domain names to the machine-readable IP addresses. When a user enters a domain name in the browser, the browser has to translate the domain name to IP address by asking the DNS infrastructure. Once the desired IP address is obtained, the user’s request is forwarded to the destination web server.
- name servers: DNS servers that store the DNS records
- resource records: The DNS databases stores domain name to IP address mappings in the form of resource records. 

### Types of resource records
- A: 